package com.tezov.tuucho.core.domain.business.jsonSchema.response

object TypeSchema {
    const val root = com.tezov.tuucho.core.domain.business.jsonSchema.material.TypeSchema.root

    object Value {
        const val response = "response"

        const val text = "text"
        const val action = "action"
    }
}
